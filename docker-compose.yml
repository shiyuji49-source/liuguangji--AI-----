# 鎏光机内测部署（单机 docker compose）：postgres + 迁移 + 应用
# 用法见 docs/部署指南.md。所有密钥从同目录 .env 读取。
services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: liuguangji
      POSTGRES_PASSWORD: ${DB_PASSWORD:?请在 .env 里设置 DB_PASSWORD}
      POSTGRES_DB: liuguangji
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U liuguangji"]
      interval: 5s
      timeout: 3s
      retries: 20

  # 一次性任务：跑数据库迁移（用 builder 层，有 drizzle-kit）
  migrate:
    build:
      context: .
      target: builder
    command: npx drizzle-kit migrate
    environment:
      DATABASE_URL: postgres://liuguangji:${DB_PASSWORD}@db:5432/liuguangji
    depends_on:
      db:
        condition: service_healthy

  # 一次性任务：把产物卷 /data/assets 归属非 root 运行用户（app）。
  # 修复历史 root:root 卷 + 新卷兜底；否则出图写文件 EACCES → "服务器内部错误"。
  chown-assets:
    build:
      context: .
      target: runner
    user: root
    entrypoint: []
    command: ["sh", "-c", "mkdir -p /data/assets && chown -R app:app /data/assets"]
    volumes:
      - assets:/data/assets

  app:
    build:
      context: .
      target: runner
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      # 覆盖 .env 中的本地库地址，指向 compose 内的 db 服务
      DATABASE_URL: postgres://liuguangji:${DB_PASSWORD}@db:5432/liuguangji
      # NextAuth 在 IP/反代后面需要信任 Host
      AUTH_TRUST_HOST: "true"
      NODE_ENV: production
      # 生成产物（图片/视频）落持久卷，否则重建容器即丢
      STORAGE_DRIVER: local
      STORAGE_LOCAL_DIR: /data/assets
    volumes:
      - assets:/data/assets
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      chown-assets:
        condition: service_completed_successfully

volumes:
  pgdata:
  assets:
